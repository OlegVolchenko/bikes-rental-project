
version: 2

models:
    - name: bikes_rental_fact
      description: "Bikes rentals model "
      columns:
          - name: rental_id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null



sources:
    - name: staging
      #For bigquery:
      database: zoomcamp-olvol3
      schema: br_staged

      # loaded_at_field: record_loaded_at
      tables:
        - name: br_trips

